#VBF invisble k-factor producer

##Introduction

The `version-0.9` branch contains an older version, not fully described, but has more features. 
The output for the commons functions to `master` will be the same though